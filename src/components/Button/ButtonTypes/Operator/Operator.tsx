import { Button } from "../../Button"

export class Operator extends Button {
    constructor(props: any) {
        /* istanbul ignore next */
        /* super transpiles to  var _this = _super.call(this) || this; */
        super(props)
    }
}