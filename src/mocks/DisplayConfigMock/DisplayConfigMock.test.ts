import { DisplayConfigMock } from "./DisplayConfigMock"

describe("DisplayConfigMock", () => {
    const config = DisplayConfigMock

    it("is defined", () => {
        expect(config).toBeDefined()
    })
})